{
  "name": "insmile-ai",
  "version": "1.0.0",
  "description": "AI-powered dental scan analysis platform",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "start:server": "npm run dev --prefix server",
    "start:client": "npm start --prefix client",
    "start": "concurrently -n server,client -c blue,green \"npm run start:server\" \"npm run start:client\"",
    "seed": "node server/scripts/seed.js",
    "vercel-build": "npm install --prefix client && npm run build --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "keywords": [
    "dental",
    "AI",
    "healthcare",
    "scan analysis",
    "treatment planning"
  ],
  "author": "Your Name",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
